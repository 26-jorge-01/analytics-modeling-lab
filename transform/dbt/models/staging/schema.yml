version: 2

models:
  - name: stg_secop__contracts_api
    description: "Staging table for secop contracts (API), cleaned and typed from raw data."
    columns:
      - name: id_contrato
        description: "ID del contrato."
        tests:
          - not_null
          - unique
